import React from "react";

// Template Page for all pages with different content inside
function Template() {
    return(
        <div>
            Template Page
        </div>
    )
}

export default Template